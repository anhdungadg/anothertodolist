// Todo List — application logic
// Step 1 scaffold: element references and init only.
// Add/delete behavior will be implemented in a later step.

// Cache references to the DOM elements we'll work with.
const form = document.getElementById("todo-form");
const input = document.getElementById("todo-input");
const list = document.getElementById("todo-list");

function init() {
  // Wiring up event listeners and rendering will go here.
}

init();
